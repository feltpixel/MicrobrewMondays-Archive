<!DOCTYPE html>
<html lang="en">
  <head>
 
 
  </head>
  <body>
 
    <% include partials/nav %>
 
    <h1><%= title %></h1>
 
    <ul>
      <% users.forEach(function(user) { %>
        <li><%= user.username %> is a <%= user.permissions %>.</li>
      <% }); %>
    </ul>
 
  </body>
</html>